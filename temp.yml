- name: Temp tasks
  become: yes
  hosts: workers
  tasks:
    - name: start firewalld
      shell: |
        sudo iptables -t nat -I PREROUTING 1 \
        -d 136.117.221.39/32 \
        -p tcp --dport 80 \
        -j REDIRECT --to-port 30080
